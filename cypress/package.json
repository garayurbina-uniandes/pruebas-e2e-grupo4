{
  "name": "cypress",
  "version": "1.0.0",
  "description": "",
  "main": "cypress.config.js",
  "scripts": {
    "test": "./node_modules/.bin/cypress run --headed --browser chrome",
    "open": "./node_modules/.bin/cypress open",
    "test:pseudo-random": "./node_modules/.bin/cypress run --spec cypress/e2e/pseudoRandom/* --headed --browser chrome",
    "test:random": "./node_modules/.bin/cypress run --spec cypress/e2e/random/* --headed --browser chrome"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^7.6.0",
    "cypress": "^11.0.0"
  },
  "dependencies": {
    "date-fns": "^2.29.3"
  }
}
